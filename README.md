# MyLinuxConfigs

## Known issues
 - Note that if the conda environment stops showing in the bash prompt after completing the setup, ensure that the conda block occurs AFTER the MyLinuxConfigs in the ~/.bashrc file.
